From f150c001ba227a47a02d9e3540d621d0d9592aad Mon Sep 17 00:00:00 2001
From: Ricardo Salveti <ricardo@foundries.io>
Date: Mon, 27 Feb 2023 00:20:03 -0300
Subject: [PATCH] configs: mt8195: add USB as possible boot device

Add USB as possible boot medium for MT8195.

Signed-off-by: Ricardo Salveti <ricardo@foundries.io>
---
 include/configs/mt8195.h | 7 +++++++
 1 file changed, 7 insertions(+)

diff --git a/include/configs/mt8195.h b/include/configs/mt8195.h
index 066bd4fd98..a4380b5464 100644
--- a/include/configs/mt8195.h
+++ b/include/configs/mt8195.h
@@ -26,6 +26,12 @@
 #define BOOT_TARGET_MMC(func)
 #endif
 
+#ifdef CONFIG_CMD_USB
+#define BOOT_TARGET_USB(func) func(USB, usb, 0)
+#else
+#define BOOT_TARGET_USB(func)
+#endif
+
 #ifdef CONFIG_CMD_SCSI
 #define BOOT_TARGET_SCSI(func) func(SCSI, scsi, 2)
 #else
@@ -34,6 +40,7 @@
 
 #define BOOT_TARGET_DEVICES(func) \
 	BOOT_TARGET_MMC(func) \
+	BOOT_TARGET_USB(func) \
 	BOOT_TARGET_SCSI(func)
 
 #if !defined(CONFIG_EXTRA_ENV_SETTINGS)
-- 
2.34.1

