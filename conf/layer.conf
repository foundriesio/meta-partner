# We might have a conf and classes directory, append to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have a recipes directory, add to BBFILES
BBFILES += "${LAYERDIR}/recipes*/*/*.bb ${LAYERDIR}/recipes*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-partner-toradex"
BBFILE_PATTERN_meta-partner-toradex := "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-partner-toradex = "9"

LAYERDEPENDS_meta-partner-toradex = "meta-lmp-base"
LAYERSERIES_COMPAT_meta-partner-toradex = "kirkstone"
